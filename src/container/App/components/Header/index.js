import React from 'react';
import HeaderNav from '../HeaderNav'
import './Header.css'

const Header = (props) => (
  <header id="header">
    <div className="container">
      <HeaderNav />
    </div>
  </header>
)

export default Header
